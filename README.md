Create a virtual environment 
```python3 -m venv venv```

Install spacy and pandas if not yet
```pip install spacy pandas google-cloud-translate```


Install spaCy's italian model:
```python -m spacy download it_core_news_sm```